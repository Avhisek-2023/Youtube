function ShowVideoGrid({ vids }) {
  return (
    <div className="container_ShowVideoGrid">
      {vids.map((e) => (
        <div>
            
        <div/>
      ))}
    </div>
  );
}

export default ShowVideoGrid;
